function square (num) {
    return num*num;
}


const square = (num)=>{
    return num * num;
}

const square = num => num * num; //esta arror function solo aplica cuando se tiene un solo parametro y el return se vuelve implicito.
